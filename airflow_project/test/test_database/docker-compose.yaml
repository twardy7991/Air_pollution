services:
  test_pollution_db: 
    image: postgres:16.1
    ports:
      - "5442:5432"
    container_name: test_pollution_db
    volumes:
     - ./initdb:/docker-entrypoint-initdb.d:ro
    # - ./postgresql:/var/lib/postgresql/data
    environment:
     POSTGRES_PASSWORD: postgres
     POSTGRES_USER: postgres
     POSTGRES_DB: test_pollution_db
    #networks:
    #  - db_pollution_network

# networks:
#   test_db_pollution_network:
#     external: true
    
